/*using System;
using System.Numerics;
//half and Complex
class Program
{
    static void Main()
    {
        // Convert float to Half (16-bit float)
        Half h = (Half)123.456f;

        // Print Half value (may lose precision)
        Console.WriteLine(h); // e.g., outputs: 123.44

        //complex
        var c1 = new Complex(2, 3.5); // 2 + 3.5i
        var c2 = new Complex(3, 0);   // 3 + 0i (purely real)

        Console.WriteLine($"c1 = {c1}");  // Output: (2, 3.5)
        Console.WriteLine($"c2 = {c2}");  // Output: (3, 0)

        // Example: addition
        Complex sum = c1 + c2;
        Console.WriteLine($"Sum: {sum}"); // (5, 3.5)

        // Example: magnitude (absolute value)
        Console.WriteLine($"Magnitude of c1: {c1.Magnitude}");

        // Example: phase (angle)
        Console.WriteLine($"Phase of c1: {c1.Phase} radians");

        // Addition
        Console.WriteLine($"c1 + c2 = {c1 + c2}");  // (5, 3.5)

        // Multiplication
        Console.WriteLine($"c1 * c2 = {c1 * c2}");  // (6, 10.5)
    }
}*/
