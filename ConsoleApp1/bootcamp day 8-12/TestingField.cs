/*using System;

class Program
{
    static void Main()
    {
        int ukuran = 8; // ukuran papan (8x8)

        for (int baris = 0; baris < ukuran; baris++)
        {
            for (int kolom = 0; kolom < ukuran; kolom++)
            {
                // Jika (baris + kolom) genap -> kotak hitam, else putih
                if ((baris + kolom) % 2 == 0)
                    Console.Write("[]"); // kotak hitam
                else
                    Console.Write("White"); // kotak putih
            }
            Console.WriteLine(); // ganti baris
        }

        Console.ReadKey();
    }
}*/


